node_modules/
# Single committed `.env` holds dev values (secrets redacted); edit on server.
# Ignore only local overrides / secret overlays.
.env.*.local
*.secrets
*.log
logs/
dist/
coverage/
.DS_Store
