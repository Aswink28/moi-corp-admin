# ── Company Admin Portal — frontend configuration (single .env file) ─────────
# This is the ONLY frontend env file. The app always reads it.
# Base URL of the company-admin backend API. Leave empty to use the relative
# "/api" path (dev proxy → http://localhost:6010; in a build, same-origin /api).
VITE_API_BASE_URL=
